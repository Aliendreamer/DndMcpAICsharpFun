# No Specification Changes

This change is a pure refactor. No capability requirements are added, modified, or removed.

All existing behaviour of `embedding-vector-store` and `rag-retrieval` is preserved exactly.
