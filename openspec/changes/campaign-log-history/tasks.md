## 1. CampaignLogEntry entity + payloads + EF mapping + migration

- [ ] 1.1 Add `Domain/CampaignLogEntry.cs`: `enum CampaignLogKind { Roll, Encounter }`; `sealed class CampaignLogEntry { long Id; long CampaignId; long UserId; CampaignLogKind Kind; string? Label; bool Hidden; DateTime CreatedAt; string PayloadJson; }`. Add payload records `Features/Campaigns/CampaignLogPayloads.cs`: `RollLogPayload(string Expression, string Breakdown, int Total, IReadOnlyList<int> Dice, IReadOnlyList<int> Kept, string Mode)`; `EncounterLogPayload(string Difficulty, int TotalXp, int AdjustedXp, IReadOnlyList<int> PartyLevels, IReadOnlyList<EncounterMonsterLog> Monsters, bool FullyMatched, string? Note)`; `EncounterMonsterLog(string Id, string Name, double Cr, int Xp)`.
- [ ] 1.2 In `Infrastructure/Persistence/AppDbContext.cs`: add `DbSet<CampaignLogEntry> CampaignLogEntries`; map `Kind` via `HasConversion<string>()`, `PayloadJson` as `HasColumnType("text")`, index on `(CampaignId, UserId, CreatedAt)`. Mirror the existing entity config style.
- [ ] 1.3 Generate the migration: `dotnet ef migrations add AddCampaignLogEntries` (uses `AppDbContextDesignTimeFactory`). Confirm the migration `Up` creates the table + index and `Down` drops it; commit the migration file + updated `AppDbContextModelSnapshot.cs`. Build 0/0. (No test yet — Task 2 exercises the schema via Testcontainers.)

## 2. CampaignLogRepository (real-Postgres TDD)

- [ ] 2.1 Add `Features/Campaigns/CampaignLogRepository.cs` (`IDbContextFactory<AppDbContext>`, mirror `NoteRepository`): `Task<long> AddRollAsync(long userId, long campaignId, RollResult roll, string? label, bool hidden = false)` (serialize a `RollLogPayload`); `Task<long> AddEncounterAsync(long userId, long campaignId, EncounterLogPayload payload, string? label, bool hidden)`; `Task<List<CampaignLogEntry>> GetByCampaignAsync(long campaignId, long userId)` (WHERE CampaignId & UserId, OrderByDescending CreatedAt then Id); `Task RevealAsync(long id, long campaignId, long userId)` (`ExecuteUpdateAsync` set Hidden=false WHERE Id&&CampaignId&&UserId); `Task DeleteAsync(long id, long campaignId, long userId)` (`ExecuteDeleteAsync` same WHERE). DI-register (`AddScoped`/`AddSingleton` per the repo convention — check how `NoteRepository` is registered and match).
- [ ] 2.2 Failing `CampaignLogRepositoryTests` (real Postgres via `PostgresFixture`/`TestDb`, mirror `NoteRepositoryTests` / `ChatRepositoryTests`): seed a campaign for user 1; AddRollAsync + AddEncounterAsync → GetByCampaign returns both newest-first; roll payload round-trips (deserialize `RollLogPayload` == source); encounter payload round-trips; **ownership negative** — a second user's entries are not returned by GetByCampaign(campaign, user1), and RevealAsync/DeleteAsync with (id, campaign, wrongUser) changes 0 rows / leaves the entry intact; RevealAsync flips Hidden→false; DeleteAsync removes.
- [ ] 2.3 Implement; make 2.2 pass (Docker up); build 0/0. Confirm the migration applied cleanly in the Testcontainer (the tests failing on a bad migration would surface here).

## 3. DiceRollerPanel auto-logs labelled rolls

- [ ] 3.1 Update `CompanionUI/Components/DiceRollerPanel.razor`: add `[Parameter] public long CampaignId { get; set; }` and `[Parameter] public long UserId { get; set; }` (the campaign page passes the authenticated user id). Add an optional **Label** input (free text) + quick-pick label buttons/dropdown (Attack, Damage; skill checks Deception, Insight, Intimidation, Perception, Persuasion, Stealth, Acrobatics, Athletics, Arcana, History, Investigation, Medicine, Nature, Religion, Survival, Sleight of Hand, Animal Handling, Performance; ability saves STR/DEX/CON/INT/WIS/CHA) that set the label. Inject `CampaignLogRepository`. In `Roll()`, after a successful roll, call `await LogRepo.AddRollAsync(UserId, CampaignId, res, _label, hidden:false)` (guard: only when `CampaignId > 0`). Keep the ephemeral recent list. On the CampaignDetail embed, pass `CampaignId` + the authenticated `UserId`.
- [ ] 3.2 Build 0/0 (razor compiles). Grep-confirm no HTTP/MCP added. (Interaction covered by the repository tests + Task 5 smoke.)

## 4. EncounterPanel + CampaignLog components on CampaignDetail

- [ ] 4.1 Add `CompanionUI/Components/EncounterPanel.razor`: `[Parameter] long CampaignId`, `[Parameter] long UserId`. A form — difficulty (Easy/Medium/Hard/Deadly), edition (2014/2024), optional theme. Inject `EncounterDesignService` + `CampaignLogRepository`. On **Build**: `await EncounterSvc.BuildForUserAsync(UserId, CampaignId, partyLevels:null, difficulty, edition, theme, ct)` → show the assessed difficulty + monsters + the not-fully-matched note. On **Save to log**: build an `EncounterLogPayload` from the result + `await LogRepo.AddEncounterAsync(UserId, CampaignId, payload, _label, _hidden)`. Handle errors (e.g. empty campaign → the service throws ArgumentException → show the message, no crash).
- [ ] 4.2 Add `CompanionUI/Components/CampaignLog.razor`: `[Parameter] long CampaignId`, `[Parameter] long UserId`, inject `CampaignLogRepository`. On init/refresh: `GetByCampaignAsync(CampaignId, UserId)` → render newest-first; roll rows (label · breakdown · total), encounter rows (label · difficulty · monster names); hidden entries badged/greyed with a **Reveal** button (`RevealAsync` then refresh) and a **Delete** button (`DeleteAsync` then refresh). Expose a public `RefreshAsync()` (or use an `EventCallback`) so a roll/save can refresh the log.
- [ ] 4.3 Embed `<EncounterPanel …/>` and `<CampaignLog …/>` on `CampaignDetail.razor`, passing `CampaignId` (the route `Id`) + the authenticated `UserId` (resolve from `AuthenticationStateProvider`/claims as the page already does). Wire the roller + encounter save to refresh the log (shared `CampaignLog` ref or a callback). Build 0/0.

## 5. Verify + review

- [ ] 5.1 Full build 0/0 + full suite green (incl. Testcontainers — the new repo tests + migration). `dotnet format --verify-no-changes` (normalize new files).
- [ ] 5.2 Drive it (per `run`/`verify`): if the stack is up, open a campaign, roll `1d20` labelled "Deception" → see it in the log; build + save a hidden Hard encounter → see it badged hidden → Reveal → it un-hides; delete an entry. Confirm a second user can't see/reveal it (ownership). Defer honestly if no live run; the real-Postgres repo tests carry the ownership + persistence correctness.
- [ ] 5.3 Whole-branch review (opus) — cross-check every ADDED requirement (payload round-trip, ownership scoping incl. reveal/delete negatives, rolls auto-logged, encounters explicit-save, hidden/reveal timeline). Emphasize the OWNERSHIP scoping (no cross-user leak/mutation) and the migration. Address findings; then Items A + B are done → run the finish steps (archive both changes → skill-optimizer → roadmap).
